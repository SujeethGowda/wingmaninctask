import 'package:flutter/material.dart';

abstract class Palette {
  static const accentColor = Color(0xFF1F6490);
  static const accentLight = Color(0xFF5791c1);
  static const accentDark = Color(0xFF003a62);

  static const red = Colors.red;
  static final error = Colors.red[700];
  static const green = Color(0xFF00B14C);
  static const grey = Colors.grey;
  static const black = Colors.black;
  static const white = Colors.white;
  static const transparent = Colors.transparent;

  static const whiteAccent = Color(0XFFf8f8f7);

  static const baseColor = Color(0xFF066490);
}
